export const APP_NAME = 'Insane Dream Builder';

/**
 * Deep link scheme used by React Navigation linking config
 * and by OAuth providers (Supabase).
 *
 * This MUST match the scheme declared in:
 *   - android/app/src/main/AndroidManifest.xml  (data android:scheme="...")
 *   - Supabase Dashboard URL config              (e.g. isdbapp://auth/callback)
 */
export const DEEP_LINK_SCHEME = 'isdbapp';
