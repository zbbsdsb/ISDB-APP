# 2026-06-07 Daily Memory

## ISDB-APP: RN 0.76 Downgrade + Bug Fixes

### Session Summary
User (Ceaser Zhao) asked me to: downgrade RN to stable version, fix all dependency conflicts, do code review, create code standards doc, run full tsc check, then commit in English.

### What Was Done

**1. RN Downgrade (RN 0.85.3 → 0.76.9, React 19.2.3 → 18.3.1)**
- Root cause: RN 0.85 + React 19 too aggressive, most 3rd-party libs not compatible
- Changes: `package.json`, `android/build.gradle`, `gradle-wrapper.properties`, `babel.config.js`, `jest.config.js`, `metro.config.js`, `tsconfig.json`
- Result: `npm install` succeeded, 832 packages installed
- Commit: `955c99d` "chore(deps): downgrade react-native from 0.85 to 0.76.9"

**2. Code Review (P0/P1 bugs identified)**
- P0 Bug 1: `use-auth.ts:23` — `currentSession.user as User` wrong type assertion (custom User ≠ Supabase User)
- P0 Bug 2: `supabase.ts:14` — `catch {` missing error param
- P0 Bug 3: `use-auth.ts:93-102` — `error` state exposed but `setError` not exposed
- P1 Bug 4: `auth-store.ts` — `session: any | null` should be `Session | null`
- P1 Bug 5: `use-theme.tsx:1` — unnecessary `import React` (React 18+)
- P1 Bug 6: `use-auth.ts` — `signInWithGitHub`/`signInWithDiscord` duplicated code

**3. Bug Fixes (all P0/P1)**
- Fixed all 6 bugs listed above
- `auth-store.ts`: changed `session: any` → `Session`, `user: User` → `SupabaseUser`
- `use-auth.ts`: removed `as User`, exposed `setError`, deduplicated sign-in functions
- `supabase.ts`: fixed `catch {` → `catch (_e) {`
- `use-theme.tsx`: removed `import React`, used `ReactNode` directly
- TypeScript check: 0 errors
- Commit: `fb32d08` "fix(auth): resolve P0/P1 bugs in auth flow and theme hook"

**4. Code Standards Doc**
- Created `docs/CODE_STANDARDS.md` (English)
- Covers: naming conventions, directory structure, Git commit conventions (Conventional Commits), code style rules

### Key Technical Decisions
- **RN 0.76.9 + React 18.3.1**: Stable New Architecture version, all 3rd-party libs compatible
- **`@react-native/*` packages removed**: RN 0.76 doesn't have separate `@react-native/*` packages; configs use `react-native/X` (main package) instead
- **`auth-store.ts` user type**: Changed from custom `User` (from `../types`) to `SupabaseUser` (from `@supabase/supabase-js`) — `session.user` is Auth user, not profile user

### Project State
- `android/ISDBAPP`: RN 0.76.9, all deps installed, TSC clean
- `shared/`: TSC clean
- Git: 2 commits ahead (`955c99d` downgrade, `fb32d08` bug fixes)
- **Next step**: Try running Android emulator to verify app boots; then fix P2 issues (add tests, unify shared/ deps)

---

## Session 2: P0 Bug Fixes (Code Review Continuation)

### What Was Done
User chose option A: fix all P0 bugs first before running emulator.

**P0 Bug Fixes (4 files, 1 commit):**

1. **`onboarding.tsx:239`** — `onTagsChange={(interests) => {}}` was empty function, interests never updated → fixed to `onTagsChange={handleInterestsComplete}`
2. **`use-profile.ts`** — Root cause of P0 Bug #3: `getProfile`/`checkProfileComplete` not wrapped in `useCallback`, causing `navigation/index.tsx` useEffect to re-run infinitely → wrapped all 5 functions (`getProfile`, `createProfile`, `updateProfile`, `checkUsernameAvailable`, `checkProfileComplete`) in `useCallback(fn, [])`
3. **`auth-callback.tsx`** — `setTimeout` had no cleanup, could `navigation.reset` on unmounted component → added `mountedRef` + `timeoutRef` + cleanup in useEffect
4. **`navigation/index.tsx`** — useEffect stale closure: if `user` changes mid-flight, old `checkOnboarding` call would `setNeedsOnboarding` with stale data → added `onboardingCheckRef` pattern (set `true` on cleanup, check before state update)

**Files modified:**
- `android/ISDBAPP/src/app/onboarding.tsx` (line 239 fix)
- `android/ISDBAPP/src/hooks/use-profile.ts` (full rewrite with useCallback)
- `android/ISDBAPP/src/app/auth-callback.tsx` (full rewrite with cleanup)
- `android/ISDBAPP/src/navigation/index.tsx` (useEffect fix + import React removal)

**Commit:** `a0f0615` "fix(app): resolve all P0 bugs (onboarding, auth-callback, navigation, use-profile)"

**TypeScript check:** 0 errors ✅

### Remaining P0/P1/P2 Issues
- P0: All fixed ✅
- P1: `theme-store.ts` Appearance API stub, `use-tags.ts` missing cleanup, `as never` hacks in auth-callback/landing
- P2: No ErrorBoundary, `import React` not fully cleaned, env config hardcoded

### Next Step
Run Android emulator to verify app boots without crashes. If emulator works, move to P1 fixes + architecture optimization.
