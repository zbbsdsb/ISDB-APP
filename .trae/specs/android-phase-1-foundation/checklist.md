# Oasis.ISDB - Phase 1: Foundation - Verification Checklist

- [ ] Project successfully initialized with `oasis.isdb` package name
- [ ] `@isdb/shared` package integrated and types imported without errors
- [ ] Supabase client configured and basic fetch (tags) succeeds
- [ ] Deep link `isdbapp://auth/callback?code=test` works correctly
- [ ] Navigation system renders bottom tabs and navigates between screens
- [ ] Dark/light theme toggles smoothly and preferences persist
- [ ] All base UI components render in both dark and light modes
- [ ] APK builds successfully in debug mode
- [ ] App launches without crashes on physical device/emulator
- [ ] TypeScript strict mode enabled and passes typecheck
